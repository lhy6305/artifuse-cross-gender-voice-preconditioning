# 2026-03-28

## 今日目标

- 完成 `low-rank envelope subspace v1` 的正式听审。
- 判断这条 learned latent 线是否终于摆脱 `8/8 no audible`。
- 若仍失败，及时把线性 low-rank latent 路线正式收口。

## 今日完成

- 完成 `artifacts/listening_review/stage0_speech_low_rank_envelope_listening_pack/v1/` 的正式听审。
- 完成一次全仓库健康度/规范性自检，并另行落盘：
  - `reports/daily/2026-03-28_repo_health_audit.md`
- 继续完成一轮结构整改，收敛 `speech resonance listening pack` 的重复实现：
  - 新增公共模块：`scripts/stage0_speech_resonance_pack_common.py`
  - 重构脚本：`scripts/build_stage0_speech_lpc_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_lsf_listening_pack.py`
- 继续把 `speech envelope family` 的重复实现合并到统一公共层：
  - 新增公共模块：`scripts/stage0_speech_envelope_pack_common.py`
  - 重构脚本：`scripts/build_stage0_speech_neural_envelope_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_conditioned_neural_envelope_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_probe_guided_envelope_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_conditional_envelope_transport_listening_pack.py`
- 继续把剩余 `speech representation / classic warp` listening pack 接到统一输出规范：
  - 重构脚本：`scripts/build_stage0_speech_low_rank_envelope_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_cepstral_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_vtl_warping_listening_pack.py`
  - 重构脚本：`scripts/build_stage0_speech_world_stft_delta_listening_pack.py`
- 完成长设计稿文档拆分：
  - 入口摘要保留在：`initial_design.md`
  - 冻结完整参考稿迁到：`docs/reference/initial_design_full_v1.md`
- 同步重建并刷新：
  - `artifacts/listening_review/stage0_speech_lpc_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_lsf_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_neural_envelope_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_conditioned_neural_envelope_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_probe_guided_envelope_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_conditional_envelope_transport_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_low_rank_envelope_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_cepstral_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_vtl_warping_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v1/`
- 核对听审队列，确认：
  - `8/8 reviewed`
  - `effect_audible: no=8`
- 更新：
  - `docs/48_representation_layer_low_rank_envelope_subspace_probe_v1.md`
  - `docs/01_project_overview_and_plan.md`
  - `docs/05_task_branch_map.md`
- 新增断点文档：
  - `docs/49_post_low_rank_checkpoint_move_beyond_linear_subspace_v1.md`

## 关键结论

- `low-rank envelope subspace v1` 虽然机器侧先验明显强于 `conditional transport v1 / v2`：
  - `avg auto_quant_score ≈ 52.72`
  - `avg auto_effect_score ≈ 34.92`
- 但正式听审仍然给出：
  - `8/8 no audible`
- 因此当前不能把问题理解成“reference retrieval 太弱，换到 low-rank 就已经成立”。
- 现在更接近的判断是：
  - `reference transport` 失败
  - `linear low-rank latent affine` 也失败
  - 当前线性、浅层的 envelope latent 编辑整体都还没有跨过主观可辨阈值

## 当前判断

- `linear low-rank envelope subspace` 当前应收口为 `null_result`。
- 不再继续出常规 `low-rank v2 / v3`，也不再继续抠 `latent_dims / latent_mix / variance_mix / blend`。
- 下一步主线应升级到更强的：
  - `nonlinear / neural envelope latent`
  - 或 `conditioned latent predictor / learned decoder`

## 结构整改补记

- `LPC / LSF listening pack` 原先共享大量重复的：
  - `json/rule lookup`
  - `WORLD voicing`
  - `stable LPC`
  - `overlap-add`
  - `summary / README`
- 本轮已把这些公共逻辑收敛到 `scripts/stage0_speech_resonance_pack_common.py`，避免后续两条支线再并行漂移。
- 重构后：
  - `build_stage0_speech_lpc_listening_pack.py` 缩到 `247` 行
  - `build_stage0_speech_lsf_listening_pack.py` 缩到 `310` 行
- 两条 pack 的 `summary.csv` 与 `review_queue.csv` 现已补齐 `record_id`，后续人工听审与量化回写不再只依赖 `utt_id`。
- 当前运行约定继续保持：
  - 必须使用仓库根目录的 `.\python.exe`
  - 不要混用系统 `python`

## 结构整改补记 2

- `speech envelope family` 之前也存在同样的长期维护问题：
  - `neural`
  - `conditioned neural`
  - `probe-guided`
  - `conditional transport`
  这 4 支脚本各自重复维护 `reference row 读取 / voiced cepstrum 抽取 / WORLD 对齐 / summary/README 输出 / 命名规则`。
- 本轮已把共享逻辑收敛到 `scripts/stage0_speech_envelope_pack_common.py`。
- 这一轮净效果：
  - `build_stage0_speech_neural_envelope_listening_pack.py` 降到 `354` 行
  - `build_stage0_speech_conditioned_neural_envelope_listening_pack.py` 降到 `445` 行
  - `build_stage0_speech_probe_guided_envelope_listening_pack.py` 降到 `366` 行
  - `build_stage0_speech_conditional_envelope_transport_listening_pack.py` 降到 `397` 行
- 4 条 envelope 支线的 `summary.csv` 和 `review_queue.csv` 也都已统一带上 `record_id`，避免回到只靠 `utt_id` 的旧状态。

## 结构整改补记 3

- `low-rank / cepstral / VTL / WORLD-STFT` 这 4 条支线也已收敛到统一的 pack 输出规范。
- 这一轮净效果：
  - `build_stage0_speech_low_rank_envelope_listening_pack.py` 降到 `304` 行
  - `build_stage0_speech_cepstral_listening_pack.py` 降到 `254` 行
  - `build_stage0_speech_vtl_warping_listening_pack.py` 降到 `293` 行
  - `build_stage0_speech_world_stft_delta_listening_pack.py` 降到 `241` 行
- 当前 `speech listening pack` 家族里，大部分长期维护脚本都已经统一到：
  - 稳定 `record_id`
  - 一致的 `summary.csv / review_queue.csv`
  - 一致的 rebuild README
  - 一致的文件命名规则
- 因此接下来的主要长文件风险，已经从“脚本重复实现”转移到“总览文档过长”和少数 GUI/报告脚本体量偏大。

## 文档整改补记

- 这轮重新评估后，最明确需要拆分的不是 `docs/01_project_overview_and_plan.md`，而是根目录 `initial_design.md`：
  - 它已超过 `1100` 行
  - 同时承担“入口摘要 / 完整设计 / 前期准备附录”三种职责
- 本轮已将它收敛为短摘要入口，并把完整长稿冻结到 `docs/reference/initial_design_full_v1.md`。
- 当前判断：
  - `docs/01_project_overview_and_plan.md` 仍然适合继续保留为单文件总控台
  - `docs/10_stage0_stage1_experiment_design.md` 虽长，但章节职责仍单一，暂不急拆

## 补充推进

- 已按上述约束直接新增 `neural envelope latent v1`：
  - 脚本：`scripts/build_stage0_speech_neural_envelope_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_neural_envelope_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_neural_envelope_review_gui.ps1`
  - 文档：`docs/50_representation_layer_neural_envelope_latent_probe_v1.md`
- `neural envelope latent v1` 当前实现口径：
  - 按 `dataset` 训练小型自编码器
  - 在 nonlinear latent 里做 `source_gender -> target_gender` 映射
  - 用 `decoder(target_latent) - decoder(current_latent)` 的差分回投包络空间
- `neural envelope latent v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_neural_envelope_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_neural_envelope_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 49.44`
  - `avg auto_direction_score ≈ 27.06`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 29.05`
  - `borderline = 2`
  - `fail = 6`
- 当前判断补充：
  - 这条 nonlinear latent 线已经跑通，但机器侧并没有自动优于 `linear low-rank`
  - 因此当前下一步仍应直接做 `neural v1` 的正式听审，而不是只凭机器侧先验提前否掉

## 补充结论

- `neural envelope latent v1` 的正式听审现已完成：
  - `8/8 reviewed`
  - `effect_audible: no=8`
- 这说明：
  - 当前 nonlinear latent 也没有跨过主观可辨阈值
  - 问题不只在线性 low-rank，而是当前这种“小型自编码器 + 静态 latent 映射”同样太弱
- 因此当前阶段结论继续收紧为：
  - `reference transport`：`null_result`
  - `linear low-rank latent`：`null_result`
  - `static neural latent mapping`：`null_result`
- 已新增断点文档：
  - `docs/51_post_neural_checkpoint_move_beyond_static_latent_mapping_v1.md`
- 当前新的主线口令是：
  - 不再继续抠常规 `neural v2`
  - 下一步应切到 `conditioned latent predictor / learned decoder`

## 补充推进

- 已按上述主线继续新增 `conditioned neural envelope v1`：
  - 脚本：`scripts/build_stage0_speech_conditioned_neural_envelope_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_conditioned_neural_envelope_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_conditioned_neural_envelope_review_gui.ps1`
  - 文档：`docs/52_representation_layer_conditioned_neural_envelope_probe_v1.md`
- `conditioned neural envelope v1` 当前实现口径：
  - 先训练自编码器学习 nonlinear latent
  - 再基于低阶倒谱 proxy 的跨性别最近邻伪配对，训练 `source latent -> matched target latent` 的 translator
  - 推理时用 `predictor + decoder`，不再只做 static latent centroid/std 映射
- `conditioned neural envelope v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_conditioned_neural_envelope_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_conditioned_neural_envelope_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 38.48`
  - `avg auto_direction_score ≈ 11.45`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 13.29`
  - `fail = 8`
- 当前判断补充：
  - 这条 conditioned predictor 线在方法层级上更强，但机器侧并不乐观
  - 因此当前下一步仍应直接做 `conditioned neural v1` 的正式听审，而不是只凭自动量化提前否掉

## 补充结论

- `conditioned neural envelope v1` 的正式听审现已完成：
  - `8/8 reviewed`
  - `effect_audible: no=8`
- 这说明：
  - 当前 envelope mapping 家族已经给出连续多轮负证据
  - 不只是 reference / low-rank / static neural 失败，conditioned predictor 也没有跨过阈值
- 已新增断点文档：
  - `docs/53_post_conditioned_predictor_checkpoint_move_beyond_mapping_family_v1.md`
- 当前新的主线口令是：
  - 不再继续抠 envelope mapping family
  - 下一步应切到 `probe-guided direct residual`

## 补充推进

- 已按上述主线继续新增 `probe-guided envelope v1`：
  - 脚本：`scripts/build_stage0_speech_probe_guided_envelope_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_probe_guided_envelope_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_probe_guided_envelope_review_gui.ps1`
  - 文档：`docs/54_representation_layer_probe_guided_envelope_probe_v1.md`
- `probe-guided envelope v1` 当前实现口径：
  - 先训练 `gender probe`
  - 再在推理时对当前帧包络做 bounded optimization
  - 直接优化目标方向判别分数，而不是先学 target mapping
- `probe-guided envelope v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_probe_guided_envelope_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_probe_guided_envelope_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 41.62`
  - `avg auto_direction_score ≈ 15.91`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 17.86`
  - `fail = 8`
- 当前判断补充：
  - 这条 direct residual 线已跑通，但机器侧仍偏负
  - 因此当前下一步仍应直接做 `probe-guided v1` 的正式听审

## 接班口令

> 不要继续抠 linear low-rank envelope subspace。`2026-03-28` 这轮正式听审已经给出 `8/8 no audible`，下一步该换到更强的 nonlinear / neural envelope latent 了。

## 补充结论

- `probe-guided envelope v1` 的正式听审现已补完：
  - `8/8 reviewed`
  - `effect_audible: no=8`
- 这说明：
  - 当前问题不只是 `mapping family` 太弱；
  - 即使升级到 discriminative objective，纯 `envelope-only` 原始相位重建家族也仍然没有跨过主观可辨阈值。
- 已新增断点文档：
  - `docs/55_post_probe_guided_checkpoint_move_beyond_envelope_only_v1.md`
- 当前新的主线口令是：
  - 不再继续抠 `probe-guided envelope v2`
  - 下一步应升级到 `joint source-filter residual`

## 补充推进

- 已按上述主线继续新增 `source-filter residual v1`：
  - 脚本：`scripts/build_stage0_speech_source_filter_residual_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_source_filter_residual_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_source_filter_residual_review_gui.ps1`
  - 文档：`docs/56_representation_layer_source_filter_residual_probe_v1.md`
- `source-filter residual v1` 当前实现口径：
  - 不再只优化低阶包络；
  - 改为联合优化 `low-order cepstral envelope + voiced harmonic residual band proxy`；
  - 仍保留原始相位重建，避免回到 `WORLD full resynthesis` 的副作用。
- `source-filter residual v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_source_filter_residual_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_source_filter_residual_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 39.19`
  - `avg auto_direction_score ≈ 12.70`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 13.89`
  - `fail = 8`
- 当前判断补充：
  - 这条 joint source/filter 线已经真正落地，但首轮机器侧先验仍偏负；
  - 当前正式听审现已补完：`8/8 reviewed`、`effect_audible no=8`
  - 因此这条轻量 `joint source-filter residual` 线当前也应收口为 `null_result`。

## 流程调整

- 已按本轮结论新增 machine-first gate：
  - 文档：`docs/57_machine_first_review_gate_v1.md`
  - 脚本：`scripts/build_listening_machine_gate_report.py`
  - 报告：`artifacts/machine_gate/v1/`
- 当前 gate 阈值固定为：
  - `avg_auto_quant_score >= 65`
  - `avg_auto_direction_score >= 45`
  - `avg_auto_effect_score >= 45`
  - 并且 `top_auto_quant_score >= 75` 或 `strongish_rows >= 2`
- 基于现有历史包回放，这套 gate 当前结果是：
  - `allow_human_review = 4`
  - `skip_human_review = 20`
- 因此从这个断点开始：
  - 默认不再把低先验包直接送人工；
  - 后续新包先过 machine gate，再决定是否正式听审。

## 补充推进

- 已按 machine-first 新流程继续新增 `LSF machine sweep v2`：
  - 脚本：`scripts/run_lsf_machine_sweep.py`
  - 总表：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v2/lsf_machine_sweep_pack_summary.csv`
  - 摘要：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v2/LSF_MACHINE_SWEEP_V2.md`
- 本轮共跑 `6` 个 `LSF` 变体，全部通过当前 machine gate。
- 最优变体为：
  - `order18_vctk_rescue_v2e`
- 其机器侧结果为：
  - `avg auto_quant_score = 78.85`
  - `avg auto_direction_score = 68.09`
  - `avg auto_effect_score = 73.71`
  - `strong_pass = 4`
  - `borderline = 2`
  - `fail = 2`
- 相比 `LSF v1`，这版最关键的变化不是整体更亮，而是最弱的 `VCTK masculine` 单元被明显抬起：
  - `v1: avg 47.31`
  - `v2e: avg 58.92`

## 补充结论

- `machine-first` 流程并没有把主线越筛越窄到没有候选；相反，它成功把 `LSF` 这条早期高先验线重新筛回来了。
- 当前最合理的动作不是再开新家族，而是把 sweep 最优变体正式收敛成标准候选并进入人工。
- 已据此新增：
  - 配置：`experiments/stage0_baseline/v1_full/speech_lsf_resonance_candidate_v2.json`
  - 文档：`docs/58_representation_layer_lsf_probe_v2.md`
- 已完成正式包导出：
  - `artifacts/listening_review/stage0_speech_lsf_listening_pack/v2/`
- 当前接班口令改为：
  - 先完成 `LSF v2` 的正式听审
  - 若主观仍然不给正证据，再把 `LSF` 整条线彻底收口

## 补充听审结论

- `LSF v2` 的正式听审现已完成：
  - `8/8 reviewed`
  - `effect_audible: yes=4, maybe=4, no=0`
  - `strength_fit: too_weak=5`
  - `artifact_issue: yes=1`
- 当前人工结论与备注可以收敛为：
  - `LSF` 这条线已经第一次稳定跨过“整包不可辨”；
  - 但主观主结论仍然是“整体偏弱”；
  - 同时在一条 `Libri feminine` 样本上出现一次瞬时明显伪影。

## 补充推进 2

- 已按上述备注继续新增 `LSF machine sweep v3`：
  - 脚本：`scripts/run_lsf_machine_sweep.py --preset v3`
  - 总表：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v3/lsf_machine_sweep_pack_summary.csv`
  - 摘要：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v3/LSF_MACHINE_SWEEP_V3.md`
- 本轮最优变体为：
  - `order20_rescue_v3e`
- 其机器侧结果为：
  - `avg auto_quant_score = 82.81`
  - `avg auto_direction_score = 74.67`
  - `avg auto_effect_score = 78.63`
  - `strong_pass = 6`
  - `borderline = 2`
  - `fail = 0`
- 当前这版已经正式收敛为：
  - 配置：`experiments/stage0_baseline/v1_full/speech_lsf_resonance_candidate_v3.json`
  - 文档：`docs/59_representation_layer_lsf_probe_v3.md`
  - 正式包：`artifacts/listening_review/stage0_speech_lsf_listening_pack/v3/`

## 当前接班口令

- 不要再把 `LSF v2` 当成“待听审包”；它已经听完，结论是“可辨但偏弱”。
- 下一步直接做 `LSF v3` 的正式听审：
  - `.\scripts\open_stage0_speech_lsf_review_gui.ps1 -PackVersion v3`

## 补充听审结论 2

- `LSF v3` 的正式听审现已完成：
  - `8/8 reviewed`
  - `effect_audible: yes=4, no=4`
  - `artifact_issue: slight=2`
  - `strength_fit: too_weak=6`
- 当前主观补充结论是：
  - `female -> male` 前几条样本出现沉闷 / 瓶子音；
  - 同时有轻微可感知伪影；
  - 因此问题不只是“还能不能再加力”，而是当前 `male` 方向本身被定义错了。

## 补充推进 3

- 已新增频谱诊断脚本：
  - `scripts/plot_lsf_review_diagnostics.py`
- 已完成 `LSF v3` 的频谱诊断输出：
  - `artifacts/diagnostics/lsf_review_v3/`
- 当前诊断明确显示：`female -> male` 四条样本平均为
  - `0-1.5k = +0.07 dB`
  - `1.5-3k = -4.11 dB`
  - `3-8k = -1.40 dB`
- 这说明当前实现虽然不是字面意义上“删掉高频”，但在工程效果上，确实把 `male` 近似成了中高频系统性下压。

## 补充整改

- 已修改机器量化逻辑：
  - 文件：`scripts/build_stage0_rule_review_queue.py`
  - 对 `brightness_down` 新增 `presence_drop_gt_2db` 与 `brilliance_drop_gt_0p75db` 惩罚
- 已按上述诊断继续新增 `LSF selective-shift v4` machine-only sweep：
  - 脚本：`scripts/run_lsf_machine_sweep.py --preset v4`
  - 总表：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v4/lsf_machine_sweep_pack_summary.csv`
- 当前最佳变体为：
  - `mid_only_v4b`
- 但当前结论是：
  - `保 F3` 后高频塌陷有所缓解；
  - `1.5-3k` 仍系统性下压过多；
  - 因此 `v4` 暂不直接晋级人工。

## 当前接班口令 2

- 不要再把 `female -> male` 等价成整体 `brightness_down`。
- 下一步应改成更窄的 `presence-preserving tract shift`，优先约束：
  - `3-8k` 基本不下压
  - `1.5-3k` 不再出现系统性 `-3~-6 dB` 下降

## 补充推进 4

- 已在 `LSF` 实现里新增原始中高频旁路参数：
  - `preserve_original_from_hz`
  - `preserve_original_full_hz`
  - `preserve_original_mix`
- 对应实现文件：
  - `scripts/build_stage0_speech_lsf_listening_pack.py`
- 已继续完成 `v5 presence-bypass` machine-only sweep：
  - 脚本：`scripts/run_lsf_machine_sweep.py --preset v5`
  - 总表：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v5/lsf_machine_sweep_pack_summary.csv`

## 补充结论 3

- `v5` 当前已经给出明确负证据：
  - 高频旁路确实能减轻 `3-8k` 下压；
  - 但 `female -> male` 的方向性与 effect 同时大幅下滑；
  - 当前最佳变体也只有 `borderline_review_optional`，不通过主 gate。
- 这说明：
  - `brightness_down + high-band bypass` 不是正确主线；
  - 当前真正需要重做的，不是保真参数，而是 `male` 方向目标函数本身。

## 当前接班口令 3

- 不要继续给 `v5` 开人工听审。
- 下一步应进入新的 machine-only 假设：
  - 不再把 `male` 近似成 `brightness_down`
  - 不再只靠 `LSF` 中心位移 + bypass
  - 应转向 `pair-spacing / bandwidth / lower-formant geometry` 这类新的 masculine 代理

## 补充推进 5

- 已在 `LSF` 成对编辑里新增 `pair_width_ratios`：
  - 文件：`scripts/build_stage0_speech_lsf_listening_pack.py`
- 已在自动量化里新增 `formant_lowering_preserve_air` 评分分支：
  - 文件：`scripts/build_stage0_rule_review_queue.py`
- 已继续完成 `v6` machine-only sweep：
  - 脚本：`scripts/run_lsf_machine_sweep.py --preset v6`
  - 总表：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v6/lsf_machine_sweep_pack_summary.csv`

## 补充结论 4

- `v6` 当前已经给出第一版新的高先验 masculine family：
  - 不再用 `brightness_down`
  - 改成 `formant_lowering_preserve_air`
  - 把动作集中在 `F2 + pair width`
- 当前最佳变体是：
  - `lower_geom_v6b`
- 它的机器侧结果为：
  - `avg auto_quant_score = 81.13`
  - `avg auto_direction_score = 70.97`
  - `avg auto_effect_score = 87.46`
  - `fail = 0`
- 这说明当前新的 masculine 目标函数至少没有在 machine gate 上被旧问题拖死。

## 当前接班口令 4

- 下一步直接做 `LSF v6` 的正式听审：
  - `.\scripts\open_stage0_speech_lsf_review_gui.ps1 -PackVersion v6`

## 补充推进 6

- `LSF v6` 的正式听审已经完成：
  - `artifacts/listening_review/stage0_speech_lsf_listening_pack/v6/listening_review_queue.csv`
  - 结果：`8/8 reviewed`
  - `effect_audible = yes 2 / maybe 6 / no 0`
  - `strength_fit = too_weak 8 / 8`
- 已在 machine gate 汇总里加入“已听审包统一 too_weak 时，下一轮先提强再送人工”的流程信号：
  - 文件：`scripts/build_listening_machine_gate_report.py`
  - 新字段：`reviewed_too_weak_rows`、`strength_escalation_recommendation`、`strength_escalation_reason`
- 已修正 `LSF` sweep 脚本的规则命名延续问题：
  - 文件：`scripts/run_lsf_machine_sweep.py`
  - 作用：从非 `v1` 基线继续 sweep 时，不再把旧版本后缀错误保留到新变体里
- 已继续完成 `v7` machine-only sweep：
  - 脚本：`scripts/run_lsf_machine_sweep.py --preset v7`
  - 总表：`experiments/stage0_baseline/v1_full/lsf_machine_sweep_v7/lsf_machine_sweep_pack_summary.csv`
  - 机器汇总：`artifacts/machine_gate/v1/LISTENING_MACHINE_GATE_REPORT.md`
- 已把最佳变体正式收敛为：
  - `experiments/stage0_baseline/v1_full/speech_lsf_resonance_candidate_v7.json`
- 已生成标准听审包并完成 GUI smoke：
  - `artifacts/listening_review/stage0_speech_lsf_listening_pack/v7/`
  - `.\scripts\open_stage0_speech_lsf_review_gui.ps1 -PackVersion v7 -AutoCloseMs 1200`

## 补充结论 5

- `v6` 的失败模式已经明确不是“路线错”或“伪影主导”，而是统一偏弱。
- 因此当前最合理的动作不是换 family，而是把正式听审前的目标改成“先提整包强度，再验证是否进入更合格的主观区间”。
- `v7` 采用 `balanced_strong_v7d` 作为正式候选，原因是它同时提高了 feminine 与 masculine 强度，避免下一轮再次出现“整包 too_weak”。
- `v7` 当前机器侧结果为：
  - `avg auto_quant_score = 86.47`
  - `avg auto_direction_score = 81.05`
  - `avg auto_effect_score = 97.17`
  - `fail = 0`

## 当前接班口令 5

- 下一步直接做 `LSF v7` 的正式听审：
  - `.\scripts\open_stage0_speech_lsf_review_gui.ps1 -PackVersion v7`
