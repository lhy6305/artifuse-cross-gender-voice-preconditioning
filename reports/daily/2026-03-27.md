# 2026-03-27

## 今日目标

- 追查 `stage0` 共用听审 GUI 的播放按钮口径不一致问题是否影响历史结论。
- 把听审包迁出 `tmp/`，放到可长期追查的正式目录。
- 对受影响分支重导并完成复听，判断是否有被误否定的路线。

## 今日完成

- 追查旧版 `stage0_rule_review_gui.py`，确认旧 GUI 在 `stage0` 模式下同时暴露了两颗实际都播放原音的按钮：
  - `播放源音频 -> input_audio`
  - `播放原音 -> original_copy`
- 进一步核对 `stage0` 各试听包生成脚本，确认 `original_copy` 本来就是 `input_audio` 的原音副本，说明旧 GUI 的确存在“按钮命名与实际口径不一致”的问题。
- 修正共用 GUI：
  - `stage0` 模式下不再同时暴露两颗都播原音的按钮
  - 现在只保留清晰的 `原音 / 处理音` 对照
  - `stage1 cascade` 模式仍保留 `源音 / 修正后音频` 的独立口径
- 将听审包与听审结论的正式落点从 `tmp/` 迁到 `artifacts/listening_review/`。
- 新增正式说明：`artifacts/listening_review/README.md`
- 检查 `.gitignore`，确认：
  - `artifacts/listening_review/**/original/` 与 `processed/` 中的音频仍被全局音频规则忽略
  - `csv` / `md` 等听审结论文件不会被忽略，可用于恢复仓库进度
- 重导受影响、需要复听的 4 个分支到正式目录：
  - `artifacts/listening_review/stage0_speech_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_resonance_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_formant_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v1/`
- 以上 4 个 PowerShell 入口已完成 `AutoCloseMs` smoke。
- 重跑听审汇总脚本，最新汇总位于：
  - `artifacts/listening_review_rollup/v1/listening_review_row_rollup.csv`
  - `artifacts/listening_review_rollup/v1/listening_review_pack_rollup.csv`
  - `artifacts/listening_review_rollup/v1/LISTENING_REVIEW_ROLLUP.md`

## 关键结论

- 旧 GUI 的按钮口径问题确实存在，因此此前 `stage0` 中若仅凭“听不出差异”得出的结论，理论上存在假阴性风险。
- 但这次复听说明，这个问题并没有大面积推翻历史判断：
  - `stage0_speech_listening_pack/v1`：仍然 `8/8 audible_no`
  - `stage0_speech_resonance_listening_pack/v1`：仍然 `8/8 audible_no`
  - `stage0_speech_formant_listening_pack/v1`：仍然 `8/8 audible_no`
- `stage0_speech_world_stft_delta_listening_pack/v1` 是唯一被这次复听实质改写结论的分支：
  - `audible_yes=2`
  - `audible_maybe=2`
  - `audible_no=4`
  - `direction_yes=2`
  - 未出现显式 `artifact_issue`
- 因此 `WORLD-guided STFT delta v1` 现在不应继续被归入 `null_result`，而应上调为 `watch`。
- 但这条线当前仍然不是强证据：
  - 可辨识样本比例仍低
  - 已标记的可辨识样本多数仍是 `too_weak`
  - 还不足以直接压过 `envelope warp v1` 成为新的主线方法

## 当前判断

- 当前模块本体分支可按三档理解：
  - `null_result`：`speech static EQ / resonance tilt / formant anchor`
  - `watch`：`envelope warp v1`、`WORLD-guided STFT delta v1`
  - `reject`：`vocal-tract morph v1`
- `envelope warp v2` 仍保留为 `watch_with_risk`，不建议继续沿原方向硬推。
- `stage1 cascade` 仍只适合作为次级参考，不应先行驱动路线切换。

## 下一步

- 先把 `envelope warp v1` 与 `WORLD-guided STFT delta v1` 放到同一比较面上，做一次短结论对照。
- 明确二者中谁更值得保留到下一轮；如果两者都证据不足，就正式收缩到“安全归一化”。

## 补充推进

- 已新增并行比较清单：`docs/34_stage0_parallel_envelope_vs_world_comparison_v1.md`
- 已新增两份候选配置：
  - `experiments/stage0_baseline/v1_full/speech_envelope_warp_candidate_v3.json`
  - `experiments/stage0_baseline/v1_full/speech_world_stft_delta_candidate_v2.json`
- 已导出两份正式听审包：
  - `artifacts/listening_review/stage0_speech_envelope_listening_pack/v3/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v2/`
- 已导出对应听审队列与量化摘要，并完成两条 PowerShell 入口的 `AutoCloseMs` smoke：
  - `.\scripts\open_stage0_speech_envelope_review_gui.ps1 -PackVersion v3`
  - `.\scripts\open_stage0_speech_world_stft_delta_review_gui.ps1 -PackVersion v2`
- 补充：`v3 / v2` 听审后已确认变化仍偏弱，因此继续导出更强的 `audibility stress test` 包：
  - `artifacts/listening_review/stage0_speech_envelope_listening_pack/v4/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v3/`
- 当前听审口径补充为：
  - 先判断是否稳定可辨
  - 轻微整体音色偏移不直接算 `artifact_issue`
  - 只有明确噪声、振铃、破碎感、瞬态脏化再记为伪影
- 后续 `v4 / v3` 听审又新增两条关键结论：
  - `envelope warp v4` 已能稳定听出，但主观方向整体反了
  - `WORLD-guided STFT delta v3` 主观方向正确，但仍不够强
- 因此已继续导出下一轮：
  - `artifacts/listening_review/stage0_speech_envelope_listening_pack/v5/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v4/`
- 当前明确约束：
  - `envelope` 以主观方向为准，暂不信任自动 direction 指标
  - `world` 量化仍只作为改变量参考，不作为方向判定主依据
- 最新 `v5 / v4` 听审收口如下：
  - `envelope v5`：主观方向已纠正，且几乎没有明显伪影；性别变化已明显可辨，但听感更像整体音调/音色重心被拉动，关键共鸣改变仍不够
  - `world v4`：伪影已明显增强，但性别改变幅度仍不高
- 对应 pack 级统计：
  - `stage0_speech_envelope_listening_pack/v5`：`audible_yes=5`、`audible_no=3`、`artifact_slight=2`
  - `stage0_speech_world_stft_delta_listening_pack/v4`：`audible_yes=4`、`audible_maybe=4`、`artifact_yes=4`、`artifact_slight=4`
- 阶段判断更新：
  - `envelope` 保留为 `watch_with_risk`，但风险点改写为“更像音调感/整体音色拉伸，而非命中共鸣目标”
  - `world_stft_delta` 下调为 `reject`
- 下一步建议：
  - 停止继续扩 `world_stft_delta`
  - 把 `envelope` 仅作为“可辨识参考上限”
  - 新 pivot 应转到更显式的共鸣/formant 结构修改，并尽量锁住 `f0` 与谐波感
- 已按上述建议新增：
  - `experiments/stage0_baseline/v1_full/speech_formant_anchor_candidate_v2.json`
  - `docs/35_stage0_post_envelope_world_formant_pivot_v1.md`
  - `artifacts/listening_review/stage0_speech_formant_listening_pack/v2/`
- 相关入口已升级支持版本参数并完成 smoke：
  - `.\scripts\open_stage0_speech_formant_review_gui.ps1 -PackVersion v2`
