# 2026-03-27

## 今日目标

- 追查 `stage0` 共用听审 GUI 的播放按钮口径不一致问题是否影响历史结论。
- 把听审包迁出 `tmp/`，放到可长期追查的正式目录。
- 对受影响分支重导并完成复听，判断是否有被误否定的路线。

## 今日完成

- 追查旧版 `stage0_rule_review_gui.py`，确认旧 GUI 在 `stage0` 模式下同时暴露了两颗实际都播放原音的按钮：
  - `播放源音频 -> input_audio`
  - `播放原音 -> original_copy`
- 进一步核对 `stage0` 各试听包生成脚本，确认 `original_copy` 本来就是 `input_audio` 的原音副本，说明旧 GUI 的确存在“按钮命名与实际口径不一致”的问题。
- 修正共用 GUI：
  - `stage0` 模式下不再同时暴露两颗都播原音的按钮
  - 现在只保留清晰的 `原音 / 处理音` 对照
  - `stage1 cascade` 模式仍保留 `源音 / 修正后音频` 的独立口径
- 将听审包与听审结论的正式落点从 `tmp/` 迁到 `artifacts/listening_review/`。
- 新增正式说明：`artifacts/listening_review/README.md`
- 检查 `.gitignore`，确认：
  - `artifacts/listening_review/**/original/` 与 `processed/` 中的音频仍被全局音频规则忽略
  - `csv` / `md` 等听审结论文件不会被忽略，可用于恢复仓库进度
- 重导受影响、需要复听的 4 个分支到正式目录：
  - `artifacts/listening_review/stage0_speech_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_resonance_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_formant_listening_pack/v1/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v1/`
- 以上 4 个 PowerShell 入口已完成 `AutoCloseMs` smoke。
- 重跑听审汇总脚本，最新汇总位于：
  - `artifacts/listening_review_rollup/v1/listening_review_row_rollup.csv`
  - `artifacts/listening_review_rollup/v1/listening_review_pack_rollup.csv`
  - `artifacts/listening_review_rollup/v1/LISTENING_REVIEW_ROLLUP.md`

## 关键结论

- 旧 GUI 的按钮口径问题确实存在，因此此前 `stage0` 中若仅凭“听不出差异”得出的结论，理论上存在假阴性风险。
- 但这次复听说明，这个问题并没有大面积推翻历史判断：
  - `stage0_speech_listening_pack/v1`：仍然 `8/8 audible_no`
  - `stage0_speech_resonance_listening_pack/v1`：仍然 `8/8 audible_no`
  - `stage0_speech_formant_listening_pack/v1`：仍然 `8/8 audible_no`
- `stage0_speech_world_stft_delta_listening_pack/v1` 是唯一被这次复听实质改写结论的分支：
  - `audible_yes=2`
  - `audible_maybe=2`
  - `audible_no=4`
  - `direction_yes=2`
  - 未出现显式 `artifact_issue`
- 因此 `WORLD-guided STFT delta v1` 现在不应继续被归入 `null_result`，而应上调为 `watch`。
- 但这条线当前仍然不是强证据：
  - 可辨识样本比例仍低
  - 已标记的可辨识样本多数仍是 `too_weak`
  - 还不足以直接压过 `envelope warp v1` 成为新的主线方法

## 当前判断

- 当前模块本体分支可按三档理解：
  - `null_result`：`speech static EQ / resonance tilt / formant anchor`
  - `watch`：`envelope warp v1`、`WORLD-guided STFT delta v1`
  - `reject`：`vocal-tract morph v1`
- `envelope warp v2` 仍保留为 `watch_with_risk`，不建议继续沿原方向硬推。
- `stage1 cascade` 仍只适合作为次级参考，不应先行驱动路线切换。

## 下一步

- 先把 `envelope warp v1` 与 `WORLD-guided STFT delta v1` 放到同一比较面上，做一次短结论对照。
- 明确二者中谁更值得保留到下一轮；如果两者都证据不足，就正式收缩到“安全归一化”。

## 补充推进

- 已新增并行比较清单：`docs/34_stage0_parallel_envelope_vs_world_comparison_v1.md`
- 已新增两份候选配置：
  - `experiments/stage0_baseline/v1_full/speech_envelope_warp_candidate_v3.json`
  - `experiments/stage0_baseline/v1_full/speech_world_stft_delta_candidate_v2.json`
- 已导出两份正式听审包：
  - `artifacts/listening_review/stage0_speech_envelope_listening_pack/v3/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v2/`
- 已导出对应听审队列与量化摘要，并完成两条 PowerShell 入口的 `AutoCloseMs` smoke：
  - `.\scripts\open_stage0_speech_envelope_review_gui.ps1 -PackVersion v3`
  - `.\scripts\open_stage0_speech_world_stft_delta_review_gui.ps1 -PackVersion v2`
- 补充：`v3 / v2` 听审后已确认变化仍偏弱，因此继续导出更强的 `audibility stress test` 包：
  - `artifacts/listening_review/stage0_speech_envelope_listening_pack/v4/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v3/`
- 当前听审口径补充为：
  - 先判断是否稳定可辨
  - 轻微整体音色偏移不直接算 `artifact_issue`
  - 只有明确噪声、振铃、破碎感、瞬态脏化再记为伪影
- 后续 `v4 / v3` 听审又新增两条关键结论：
  - `envelope warp v4` 已能稳定听出，但主观方向整体反了
  - `WORLD-guided STFT delta v3` 主观方向正确，但仍不够强
- 因此已继续导出下一轮：
  - `artifacts/listening_review/stage0_speech_envelope_listening_pack/v5/`
  - `artifacts/listening_review/stage0_speech_world_stft_delta_listening_pack/v4/`
- 当前明确约束：
  - `envelope` 以主观方向为准，暂不信任自动 direction 指标
  - `world` 量化仍只作为改变量参考，不作为方向判定主依据
- 最新 `v5 / v4` 听审收口如下：
  - `envelope v5`：主观方向已纠正，且几乎没有明显伪影；性别变化已明显可辨，但听感更像整体音调/音色重心被拉动，关键共鸣改变仍不够
  - `world v4`：伪影已明显增强，但性别改变幅度仍不高
- 对应 pack 级统计：
  - `stage0_speech_envelope_listening_pack/v5`：`audible_yes=5`、`audible_no=3`、`artifact_slight=2`
  - `stage0_speech_world_stft_delta_listening_pack/v4`：`audible_yes=4`、`audible_maybe=4`、`artifact_yes=4`、`artifact_slight=4`
- 阶段判断更新：
  - `envelope` 保留为 `watch_with_risk`，但风险点改写为“更像音调感/整体音色拉伸，而非命中共鸣目标”
  - `world_stft_delta` 下调为 `reject`
- 下一步建议：
  - 停止继续扩 `world_stft_delta`
  - 把 `envelope` 仅作为“可辨识参考上限”
  - 新 pivot 应转到更显式的共鸣/formant 结构修改，并尽量锁住 `f0` 与谐波感
- 已按上述建议新增：
  - `experiments/stage0_baseline/v1_full/speech_formant_anchor_candidate_v2.json`
  - `docs/35_stage0_post_envelope_world_formant_pivot_v1.md`
  - `artifacts/listening_review/stage0_speech_formant_listening_pack/v2/`
- 相关入口已升级支持版本参数并完成 smoke：
  - `.\scripts\open_stage0_speech_formant_review_gui.ps1 -PackVersion v2`
- 最新听审结果：
  - `stage0_speech_formant_listening_pack/v2`：`8/8 audible_no`
  - 未出现值得记录的伪影问题
- 这说明：
  - 即使改成更强的三锚点 formant-aware 原型，当前实现仍然不可感知
  - 当前不应继续在 `stage0` 轻量前置器上新增同族变体
- 当前阶段正式收口建议：
  - `envelope v5` 只保留为“可辨识上限参考”
  - `world_stft_delta` 维持 `reject`
  - `formant-aware v2` 归入 `null_result`
  - 下一步转向“安全归一化 + 下游模型承担主变换”，或等待新的方法级 pivot
- 现已将该“方法级 pivot”具体化为表示层转型：
  - 新增文档：`docs/36_representation_layer_pivot_v1.md`
  - 新增脚本：`scripts/run_representation_layer_probe.py`、`scripts/run_representation_layer_probe.ps1`
- 已完成两轮表示层 probe：
  - `experiments/representation_layer/v1_fixed_eval_pilot/`
  - `experiments/representation_layer/v1_clean_speech_probe/`
- 当前 probe 初步结论：
  - `WORLD envelope` 分离度偏弱，不适合作为当前优先表示
  - `LPC envelope` 在 `LibriTTS-R` 上分离度最强
  - `MFCC / cepstral proxy` 在两个 speech 数据集上的稳定性更均衡
- 因此下一步已收敛为：
  - 优先推进 `LPC / LSF`
  - 同时保留 `cepstral envelope` 作为第二候选
- 已继续落地 `LPC / LSF` 路线的第一版编辑原型：
  - 新增文档：`docs/37_representation_layer_lpc_edit_probe_v1.md`
  - 新增脚本：`scripts/build_stage0_speech_lpc_listening_pack.py`
  - 新增入口：`scripts/open_stage0_speech_lpc_review_gui.ps1`、`scripts/open_stage0_speech_lpc_review_gui.cmd`
  - 新增配置：`experiments/stage0_baseline/v1_full/speech_lpc_resonance_candidate_v1.json`
- 已导出正式听审包并完成入口 smoke：
  - `artifacts/listening_review/stage0_speech_lpc_listening_pack/v1/`
  - `.\scripts\open_stage0_speech_lpc_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200`
- `LPC v1` 首次导出后发现一个实质性 bug：
  - 处理音整体电平被异常压低，导致本轮听审应判无效
  - 原因是 overlap-add 归一化与全局 peak limit 联合触发，少量尖峰把整段一起缩小
  - 已在 `scripts/build_stage0_speech_lpc_listening_pack.py` 中修复，并重导同一路径下的正式听审包
  - 当前原音/处理音 `RMS` 已基本对齐，GUI 入口已再次 smoke
- 当前 `LPC v1` 机器侧先验：
  - `avg auto_quant_score ≈ 69.41`
  - `strong_pass=2`
  - `borderline=4`
  - `fail=2`
  - `preservation_score` 已恢复到 `100`
  - 风险现主要集中在 `VCTK masculine` 侧方向偏弱
- `LPC v1` 修复后已完成一轮正式听审并导出：
  - `8/8 reviewed`
  - `effect_audible: maybe=4, no=4`
  - `strength_fit: too_weak=8`
  - 主观判断：整体极弱，几乎全部接近不可感知
- 听审 GUI 顺手补了三项问题修复：
  - 播放时“增强响度并限峰值”，弱音频下不再只做无效的片段峰值归一化
  - 左侧文件列表空白点击不再误选最底部音频
  - 新增“处理音(F0 对齐原音)”试听按钮，用于排除音调变化带来的误判
- 因此当前最直接的下一步已改成：
  - 若继续保留 `LPC` 路线，则下一轮必须直接加强编辑强度
  - 否则优先转到 `cepstral envelope` 对照线
- 已继续按该判断新增更强的 `LPC v2`：
  - 配置：`experiments/stage0_baseline/v1_full/speech_lpc_resonance_candidate_v2.json`
  - 文档：`docs/38_representation_layer_lpc_edit_probe_v2.md`
  - 听审包：`artifacts/listening_review/stage0_speech_lpc_listening_pack/v2/`
- `LPC v2` 已完成：
  - 正式包导出
  - 队列与量化摘要生成
  - `.\scripts\open_stage0_speech_lpc_review_gui.ps1 -PackVersion v2 -AutoCloseMs 1200` smoke
- 当前机器侧先验比 `v1` 更强：
  - `avg auto_quant_score ≈ 80.19`
  - `avg auto_direction_score ≈ 70.49`
  - `avg auto_preservation_score ≈ 98.61`
  - `avg auto_effect_score ≈ 75.54`
  - `strong_pass=5`
  - 但 `VCTK masculine` 侧仍是主要风险点
- 当前默认下一轮听审对象已切到 `LPC v2`
- `LPC v2` 随后已完成听审并导出：
  - `effect_audible: yes=5, maybe=2, no=1`
  - `direction_correct: maybe=5`
  - `artifact_issue: yes=1, slight=5`
- 主观结论：
  - `female -> male` 更像“闷在小瓶子里说话”
  - `male -> female` 更像“刻意增加高频”
  - 两侧都显得假，且没有真正打到目标共鸣
- 因此当前 `LPC pole edit v2` 不应视为健康升级，而应归入阶段性 `reject`
- GUI 继续修正两点：
  - 左侧列表改为“按下/释放命中同一条目且几乎未拖动才选中”，继续压低误触
  - 默认音量滑块改为 `50%`，保留 `800%` 上限，同时显著加长音量条
- 已按主线切换继续新增 `cepstral envelope v1`：
  - 脚本：`scripts/build_stage0_speech_cepstral_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_cepstral_envelope_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_cepstral_review_gui.ps1`
  - 听审包：`artifacts/listening_review/stage0_speech_cepstral_listening_pack/v1/`
- 这条线的当前实现口径：
  - 基于 clean speech 参考集的 `dataset x gender` 低阶倒谱 centroid 差分
  - 只改 voiced 帧的低阶倒谱包络
  - 保留原始相位与细节
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 85.23`
  - `avg auto_direction_score ≈ 77.87`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 80.74`
  - `strong_pass=6`
  - `fail=0`
- `.\scripts\open_stage0_speech_cepstral_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` 已 smoke 通过
- 当前默认下一轮听审对象已切到 `cepstral v1`
- `cepstral v1` 随后已完成听审并导出：
  - `effect_audible: yes=1, maybe=4, no=3`
  - `direction_correct: maybe=4`
  - `artifact_issue: slight=3`
  - `strength_fit: too_weak=4`
- 主观结论：
  - 仍然怀疑方向不对
  - 影响轻微
  - 相对改变腔体，更像添加伪影
  - `male -> feminine` 侧更明显，像刻意添加高频，甚至出现轻微噪声感
- 因此当前判断：
  - `cepstral v1` 不升格为主线
  - 按听审汇总可保留为 `watch_with_risk`
  - 但工程上不建议继续沿当前实现硬推
- 为避免后续接班再次掉回局部调参，已新增显式断点文档：
  - `docs/40_representation_layer_lsf_then_vtl_checkpoint_v1.md`
- 当前固定顺序改为：
  - 先试 `LSF`
  - 如果 `LSF` 不行，再试 `VTL / tract-length warping`
  - 在这两步完成前，不再回头继续磨 `pole edit / cepstral delta`
- 已继续按该断点直接开出 `LSF v1`：
  - 脚本：`scripts/build_stage0_speech_lsf_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_lsf_resonance_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_lsf_review_gui.ps1`
  - 文档：`docs/41_representation_layer_lsf_probe_v1.md`
- `LSF v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_lsf_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_lsf_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 73.54`
  - `avg auto_direction_score ≈ 60.31`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 65.63`
  - `strong_pass=2`
  - `borderline=4`
  - `fail=2`
- 当前风险图景：
  - `LibriTTS feminine` 侧最强
  - `VCTK masculine` 侧仍最弱，当前两条都落在 `fail`
- 因此当前默认下一轮正式听审对象已改为：
  - `.\scripts\open_stage0_speech_lsf_review_gui.ps1 -PackVersion v1`
- 只有在 `LSF v1` 主观上仍然不能给出正证据时，主线才切到 `VTL / tract-length warping`
- `LSF v1` 随后已完成正式听审：
  - `8/8 reviewed`
  - `effect_audible: yes=2, maybe=2, no=4`
  - `artifact_issue: slight=3`
- 本轮人工结论进一步收口为：
  - 全部音频整体仍然太弱
  - 部分样本开始出现伪影
  - 因此 `LSF v1` 没有形成继续留在主线的正证据
- 按既定顺序约束，下一步主线切到：
  - `VTL / tract-length warping`
- 同时把共用听审 GUI 的播放语义回退到更保守口径：
  - 默认关闭 `peak_norm`
  - 默认舒适音量改为 `70%`
  - 勾选时恢复为“按最大峰值拉伸标准化”的简单语义，不再默认做额外响度增强和 limiter 语义
- 已继续按既定顺序直接开出 `VTL v1`：
  - 脚本：`scripts/build_stage0_speech_vtl_warping_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_vtl_warping_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_vtl_review_gui.ps1`
  - 文档：`docs/42_representation_layer_vtl_warping_probe_v1.md`
- `VTL v1` 当前实现口径：
  - 保留 `WORLD analysis only + 原始相位`
  - 但不再沿旧 `world_stft_delta` 做全带宽泛化差分
  - 改成只在 tract 主频段内启用的 `VTL warp`
  - 高频做渐隐，并做带内能量回平
- `VTL v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_vtl_warping_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_vtl_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验非常弱：
  - `avg auto_quant_score ≈ 36.92`
  - `avg auto_direction_score ≈ 3.73`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 20.96`
  - `fail = 8`
- 自动量化当前认为：
  - 整体改变量偏弱
  - 多数样本方向不足
  - `LibriTTS feminine` 侧甚至出现 `wrong_direction`
- 因此当前默认下一轮正式听审对象已切到：
  - `.\scripts\open_stage0_speech_vtl_review_gui.ps1 -PackVersion v1`
- `VTL v1` 随后已完成正式听审：
  - `8/8 reviewed`
  - `effect_audible: yes=3, maybe=4, no=1`
  - `artifact_issue: slight=3, no=2`
- 但备注把问题进一步暴露出来：
  - 有样本仅在局部低 `f0` 段才可辨，整段稳定性不足
  - 多条样本的 `F0` 对齐试听版本出现明显伪影
  - 部分处理音出现类似两个高低声部叠加的轻微双声感
- 因此当前判断更新为：
  - `VTL v1` 不能直接升格为健康主线
  - 它更像“可辨但机理和副作用仍未理顺”的高风险候选
- 同时，按用户要求移除了共用听审 GUI 的全部自定义键盘快捷键，后续只保留鼠标交互，避免误触
- 已继续按这些具体失效模式直接开出 `VTL v2`：
  - 配置：`experiments/stage0_baseline/v1_full/speech_vtl_warping_candidate_v2.json`
  - 文档：`docs/43_representation_layer_vtl_warping_probe_v2.md`
- `VTL v2` 相对 `v1` 的主要修正：
  - `wet_mix -> 1.0`，优先消掉 dry/wet 叠加导致的双声感
  - 提高 `voiced_smooth_frames` 和 `freq_smooth_bins`
  - 放宽 tract band 覆盖，但保持高频渐隐
  - 把带内能量回平从硬回平改成部分回平
- `VTL v2` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_vtl_warping_listening_pack/v2/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_vtl_review_gui.ps1 -PackVersion v2 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 39.26`
  - `avg auto_direction_score ≈ 0.10`
  - `avg auto_effect_score ≈ 36.86`
  - `fail = 8`
- 这说明：
  - `effect` 确实比 `v1` 更强
  - 但 `direction` 进一步恶化
  - 因此当前默认下一轮正式听审对象已切到：
    - `.\scripts\open_stage0_speech_vtl_review_gui.ps1 -PackVersion v2`
- `VTL v2` 随后已完成正式听审：
  - `8/8 reviewed`
  - `effect_audible: yes=5, maybe=2, no=1`
  - `artifact_issue: slight=5`
  - `strength_fit: too_weak=5`
- 结合备注，当前主观结论是：
  - 这版比 `v1` 更常能听见
  - 但整体仍然偏弱
  - 部分样本仍有轻微伪影
  - 因此仍未跨过“稳定、方向可信、伪影可控”的门槛
- 同时已按用户要求修改辅助试听按钮语义：
  - 不再用 `phase vocoder` 做“仅变调”
  - 改成对处理音再做一次全局变速/变调
  - 明确接受 `pitch` 与 `duration` 一起变化
  - 只作为辅助排除整体音高偏移，不代表原处理音本体
- 到这里为止，当前判断进一步收口为：
  - 不再继续抠经典 `LSF / VTL` warp 的局部参数
  - 下一步应升级到更高层表示路线
- 已继续按该判断新增 `conditional_envelope_transport v1`：
  - 脚本：`scripts/build_stage0_speech_conditional_envelope_transport_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_conditional_envelope_transport_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_conditional_envelope_transport_review_gui.ps1`
  - 文档：`docs/45_representation_layer_conditional_envelope_transport_probe_v1.md`
- 这条线的当前实现口径：
  - 不再施加固定几何 warp
  - 改成按 `dataset x target_gender x f0桶` 建目标参考库
  - 在待处理语音的 voiced 帧上用低阶倒谱 `content proxy` 做最近邻参考检索
  - 只搬运平滑谱包络，重建仍保留原始相位
- `conditional_envelope_transport v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_conditional_envelope_transport_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_conditional_envelope_transport_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 当前机器侧先验很弱：
  - `avg auto_quant_score ≈ 31.76`
  - `avg auto_direction_score ≈ 1.43`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 4.44`
  - `fail = 8`
- 当前自动量化的判断是：
  - 保真没有明显问题
  - 但改变量非常小
  - 因此默认下一步不是继续盲目加参，而是先做正式听审，确认这条条件化路线是否再次被自动量化误杀
- `conditional_envelope_transport v1` 随后已完成正式听审：
  - `8/8 reviewed`
  - `effect_audible: no=8`
- 这说明：
  - `v1` 不是“方向错但可辨”
  - 而是直接不可分辨
  - 因此当前应把它收口为 `null_result`
- 按该结果，已继续开出更强的 `conditional_envelope_transport v2`：
  - 配置：`experiments/stage0_baseline/v1_full/speech_conditional_envelope_transport_candidate_v2.json`
  - 文档：`docs/46_representation_layer_conditional_envelope_transport_probe_v2.md`
  - 听审包：`artifacts/listening_review/stage0_speech_conditional_envelope_transport_listening_pack/v2/`
- `v2` 的核心变化不是单纯加力，而是把编辑语义从 `target pull` 改成：
  - `source-anchor -> target-anchor` 的局部对比 delta
  - 即先找源侧内容锚点，再找目标侧内容锚点，用二者差分作为条件化方向位移
- `conditional_envelope_transport v2` 当前已完成：
  - 正式包导出
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_conditional_envelope_transport_review_gui.ps1 -PackVersion v2 -AutoCloseMs 1200` smoke
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 35.64`
  - `avg auto_direction_score ≈ 7.18`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 9.63`
  - `fail = 8`
- 当前判断：
  - `v2` 比 `v1` 确实更强，但仍远未到机器侧正证据区间
  - 因此当前默认下一步已切到 `v2` 的正式听审
- `conditional_envelope_transport v2` 随后已完成正式听审：
  - `8/8 reviewed`
  - `effect_audible: no=8`
- 到这里为止，这条线的结论已经足够明确：
  - `v1`：`8/8 no audible`
  - `v2`：`8/8 no audible`
  - 因此 `reference envelope transport` 当前应整体收口，不再继续出 `v3`
- 已新增断点文档：
  - `docs/47_post_conditional_transport_checkpoint_move_beyond_reference_envelope_v1.md`
- 当前新的强约束是：
  - 不再沿 `reference retrieval / anchor transport` 家族继续抠参数
  - 下一步主线应切到学习型低维包络表示，如 `low-rank envelope subspace / latent envelope`
- 已按该约束继续新增 `low-rank envelope subspace v1`：
  - 脚本：`scripts/build_stage0_speech_low_rank_envelope_listening_pack.py`
  - 配置：`experiments/stage0_baseline/v1_full/speech_low_rank_envelope_candidate_v1.json`
  - 入口：`scripts/open_stage0_speech_low_rank_envelope_review_gui.ps1`
  - 文档：`docs/48_representation_layer_low_rank_envelope_subspace_probe_v1.md`
- `low-rank envelope subspace v1` 当前实现口径：
  - 先按 `dataset` 学习共享低秩包络子空间
  - 再在 latent 里做 `source_gender -> target_gender` 的 affine 映射
  - 只改平滑谱包络，重建继续保留原始相位
- `low-rank envelope subspace v1` 当前已完成：
  - 正式包导出：`artifacts/listening_review/stage0_speech_low_rank_envelope_listening_pack/v1/`
  - 量化队列与摘要生成
  - `.\scripts\open_stage0_speech_low_rank_envelope_review_gui.ps1 -PackVersion v1 -AutoCloseMs 1200` smoke
- 实现层顺手修正了一处一致性问题：
  - 共享 latent 统计统一保留在学习时的完整维度上
  - 对较保守规则只屏蔽高阶系数的回投改动，避免推理时截断 `basis / centroid / std` 后语义不一致
- 当前机器侧先验：
  - `avg auto_quant_score ≈ 52.72`
  - `avg auto_direction_score ≈ 31.09`
  - `avg auto_preservation_score = 100.00`
  - `avg auto_effect_score ≈ 34.92`
  - `borderline = 2`
  - `fail = 6`
- 当前判断：
  - 这条 learned latent 线已明显强于 `conditional transport v1 / v2`
  - 但 `direction` 仍然偏弱，还没有进入机器侧正证据区间
  - 因此当前下一步应直接做 `low-rank v1` 的正式听审，而不是继续停在 reference transport
